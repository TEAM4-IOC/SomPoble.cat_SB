package com.sompoble.cat.domain;

class HorarioTest {

    /*private Horario horario;
    private Empresa empresaMock;

    @BeforeEach
    void setUp() {
        empresaMock = Mockito.mock(Empresa.class);
        horario = new Horario("Lunes,Martes", LocalTime.of(9, 0), LocalTime.of(18, 0), empresaMock);


    }

    @Test
    void testGetters() {
        assertEquals("Lunes,Martes", horario.getDiasLaborables());
        assertEquals(LocalTime.of(9, 0), horario.getHorarioInicio());
        assertEquals(LocalTime.of(18, 0), horario.getHorarioFin());
        assertEquals(empresaMock, horario.getEmpresa());
    }

    @Test
    void testSetters() {
        horario.setDiasLaborables("Miercoles,Jueves");
        assertEquals("Miercoles,Jueves", horario.getDiasLaborables());

        horario.setHorarioInicio(LocalTime.of(8, 30));
        assertEquals(LocalTime.of(8, 30), horario.getHorarioInicio());

        horario.setHorarioFin(LocalTime.of(17, 30));
        assertEquals(LocalTime.of(17, 30), horario.getHorarioFin());
    }

    @Test
    void testToString() {
        String expectedString = "Horario{" +
                "idHorario=null, diasLaborables='Lunes,Martes', horarioInicio=09:00, horarioFin=18:00, empresa=" + empresaMock + "} ";
        assertTrue(horario.toString().contains("Lunes,Martes"));
    }*/
}